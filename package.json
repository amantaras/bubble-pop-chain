{
  "name": "bubble-pop-chain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bubble Pop Chain — mobile PWA puzzle game",
  "scripts": {
    "build:web": "node scripts/copy-web.mjs",
    "serve": "node tests/server.mjs 4173",
    "native:sync": "npm run build:web && cap sync",
    "android:sync": "npm run build:web && cap sync android",
    "android:open": "npm run android:sync && cap open android",
    "android:build": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:bundle": "npm run android:sync && cd android && ./gradlew bundleRelease",
    "ios:sync": "npm run build:web && cap sync ios",
    "ios:open": "npm run ios:sync && cap open ios",
    "ios:build": "npm run ios:sync && xcodebuild -workspace ios/App/App.xcworkspace -scheme App -configuration Debug -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test": "npm run test:unit && npm run test:e2e",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3",
    "@playwright/test": "^1.49.1",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
